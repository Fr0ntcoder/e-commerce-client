import { FC } from 'react';

import Layout from '@/components/layout/Layout';

import styles from './Home.module.scss';

const Home: FC = () => {
	return <Layout title='Главная'>Text</Layout>;
};

export default Home;
