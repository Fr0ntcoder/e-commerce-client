import { FC } from 'react';

import Layout from '@/components/layout/Layout';

import styles from './Cart.module.scss';

const Cart: FC = () => {
	return <Layout title='Корзина'>Cart</Layout>;
};

export default Cart;
